# 星标收藏同步配置
# Token 可选：不填也能跑；CI 会自动注入 GITHUB_TOKEN

title = "星标整理和分类"
output_md = "README.md"
output_html = "docs/index.html"
token_env = "GITHUB_TOKEN"

[[sources]]
username = "sky22333"
label = "主号"

# [[sources]]
# username = "user"
# label = "备用"

[classify]
max_categories = 12
min_count = 2
fallback = "language"   # language | other
sort_within = "stars"   # stars | starred_at | name
